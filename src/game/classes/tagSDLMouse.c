/* src/game/classes/tagSDLMouse.c — 1 function(s)
 * Original binary: `decompile` (Super Meat Boy, ELF64 x86-64)
 * Produced by: Ghidra 12.1.2 headless + tree generator
 * NOTE: decompiler output — types/names are best-effort, not build-verified.
 */
#include "ghidra_types.h"
#include "globals.h"
#include "game_types.h"

#include "tagSDLMouse.h"

/* ======================================================================
 * tagSDLMouse__tagSDLMouse  (Ghidra `~tagSDLMouse` @ 00571380)
 * Signature: uint8_t __thiscall ~tagSDLMouse(tagSDLMouse * self)
 * Class: tagSDLMouse
 * Calls: (none)
 * Called by: `GLOBAL_sub_I_uMasterInputFrameCounter`
 */
/* tagSDLMouse__tagSDLMouse() */

void __thiscall tagSDLMouse__tagSDLMouse(tagSDLMouse *self)

{
  return;
}
