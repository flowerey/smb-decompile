// Recovered class model for `TileLevelLayer` — from RTTI + vtable + symtab. NOT compiled.
// kind=si
#pragma once

class TileLevelLayer : public LevelRenderLayer {
public:
  virtual void ~TileLevelLayer();  // +0x10 @0x592100
  virtual void ~TileLevelLayer();  // +0x18 @0x592130
  virtual void Render();  // +0x20 @0x58d090
};
