// Recovered class model for `LightLayer` — from RTTI + vtable + symtab. NOT compiled.
// kind=si
#pragma once

class LightLayer : public RenderLayer {
public:
  virtual void ~LightLayer();  // +0x10 @0x517aa0
  virtual void ~LightLayer();  // +0x18 @0x518280
  virtual void Render();  // +0x20 @0x518020
};
