// Recovered class model for `RenderSetupLayer` — from RTTI + vtable + symtab. NOT compiled.
// kind=si
#pragma once

class RenderSetupLayer : public RenderLayer {
public:
  virtual void ~RenderSetupLayer();  // +0x10 @0x517a90
  virtual void ~RenderSetupLayer();  // +0x18 @0x518220
  virtual void Render();  // +0x20 @0x517ca0
};
