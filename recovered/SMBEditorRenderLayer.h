// Recovered class model for `SMBEditorRenderLayer` — from RTTI + vtable + symtab. NOT compiled.
// kind=si
#pragma once

class SMBEditorRenderLayer : public RenderLayer {
public:
  virtual void ~SMBEditorRenderLayer();  // +0x10 @0x49eb70
  virtual void ~SMBEditorRenderLayer();  // +0x18 @0x49f3c0
  virtual void Render();  // +0x20 @0x4b4a90
  // ---- non-virtual / static (from symtab) ----
  void SMBEditorRenderLayer();  // @0x4a9470
};
